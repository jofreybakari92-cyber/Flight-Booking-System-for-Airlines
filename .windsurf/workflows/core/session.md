---
description: Unified session management commands
---

# /session - Session Management

Manage MAx Framework sessions with unified command structure.

## Usage

```bash
# Start a new session
/session start

# End current session
/session end

# Show session status
/session status
```

## Commands

### /session start
Load persona, project context, and set session goals.

### /session end
Save progress to context-cache and update project-config.md.

### /session status
Show current session information and activity.

This replaces the old `/start-session`, `/end-session`, and `/session-status` commands.
